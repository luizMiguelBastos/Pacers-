spring:
  application:
    name: pacers

  datasource:
    url: jdbc:postgresql://localhost:5432/pacer_usuarios
    username: postgres
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.springframework.security: TRACE
    org.springframework.web: DEBUG

api:
  security:
    token:
      secret: ${JWT_SECRET}

